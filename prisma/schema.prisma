generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Question {
  id   String @id @unique
  name String @db.VarChar(255)
}

model Selectedanswer {
  id   String @id @unique
  name String @db.VarChar(255)
}
